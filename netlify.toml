[build]
  command = "npm run build"
  publish = "dist"
  
[build.environment]
  NODE_VERSION = "20"
  
# Thêm phần này để xử lý Vite đúng cách
[build]
  edge_functions = "netlify/edge-functions"

# Hoặc thử cấu hình đơn giản hơn:
[build]
  command = "CI=false npm run build"
  publish = "dist"
